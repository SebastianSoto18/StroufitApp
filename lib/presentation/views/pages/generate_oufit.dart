import 'package:flutter/material.dart';

class GenerateOufit extends StatelessWidget {
  const GenerateOufit({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("GenerarOutfit", style: TextStyle(fontSize: 24)));
  }
}
